spring:
  application:
    name: aerospike-integration

Aerospike:
  host: localhost
  port: 3000
  namespace: test
  set: employee
  ttl: 60
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always